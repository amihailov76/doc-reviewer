# doc-reviewer

Инструмент для автоматической оценки полноты инструкций в технической документации. Загружает документ (PDF, DOCX, MD, TXT), автоматически выделяет разделы с инструкциями и оценивает каждый из них по настраиваемым критериям с помощью LLM.

Разработан для технических писателей продуктов Positive Technologies.

---

## Возможности

- 📄 **Парсинг документов** — PDF, DOCX, Markdown, TXT
- 🔍 **Автоматическое определение инструкций** — морфологический анализ на русском языке (pymorphy3)
- 🤖 **Оценка через LLM** — поддержка OpenAI, Anthropic и любых OpenAI-совместимых API (включая локальные модели через Ollama)
- 📊 **Экспорт результатов** в XLS
- 📸 **Снимки и сравнение версий** — сохранение состояния оценки и сравнение между версиями документа
- ⚙️ **Управление моделями** — добавление и настройка LLM-моделей через интерфейс без редактирования файлов
- 📝 **Управление критериями** — редактирование и создание наборов критериев оценки через интерфейс
- 🚩 **Отметки ложных срабатываний** — ручная корректировка результатов оценки LLM
- 💾 **Локальное хранение** — SQLite, все данные на машине пользователя
- 📦 **Дистрибуция** — сборка в единый `.exe` через PyInstaller

---

## Стек

| Слой | Технологии |
|---|---|
| Бэкенд | Python 3.11, FastAPI, Uvicorn, SQLAlchemy, SQLite |
| Фронтенд | React 18, Vite |
| Парсинг | PyMuPDF, python-docx |
| NLP | pymorphy3 |
| LLM-клиент | httpx (OpenAI API + Anthropic API) |
| Экспорт | openpyxl |
| Сборка | PyInstaller |

---

## Быстрый старт

### Требования

- **Python 3.11** (версии 3.12+ не поддерживаются из-за PyMuPDF)
- **Node.js** 18+

### 1. Клонировать репозиторий

```bash
git clone https://github.com/<org>/doc-reviewer.git
cd doc-reviewer
```

### 2. Установить зависимости бэкенда

```bash
py -3.11 -m pip install -r requirements.txt
```

### 3. Запустить бэкенд

```bash
py -3.11 run_dev.py
# http://localhost:8000
```

Проверка: `http://localhost:8000/api/ping` → `{"status":"ok"}`

### 4. Установить зависимости фронтенда и запустить

```bash
cd frontend
npm install
npm run dev
# http://localhost:5173
```

### 5. Открыть в браузере

Перейдите на `http://localhost:5173` и настройте LLM-модель на вкладке **Настройки**.

---

## Конфигурация

### LLM-модели

Модели управляются через интерфейс (вкладка **Настройки → Модели**). При первом запуске загружаются дефолтные модели из `models.yml`.

Поддерживаемые провайдеры:

| Провайдер | Base URL | Требует ключ |
|---|---|---|
| OpenAI | `https://api.openai.com/v1` | Да |
| Anthropic | `https://api.anthropic.com/v1` | Да |
| Ollama (локальная) | `http://localhost:11434/v1` | Нет |
| Любой OpenAI-совместимый | произвольный | По конфигурации |

API-ключ вводится в форме редактирования модели и хранится в локальной БД.

### Критерии оценки

Критерии управляются через интерфейс (вкладка **Настройки → Критерии оценки**). Дефолтные критерии при первом запуске загружаются из `criteria.md`.

Формат критериев в Markdown:

```markdown
## Группа критериев

### 1.1 Название критерия
Описание того, что проверяется.
```

При изменении `criteria.md` вручную — перезапустите бэкенд, изменения подтянутся автоматически. Файл `criteria.md`, расположенный рядом с `.exe`, имеет приоритет над встроенной версией.

---

## Структура проекта

```
doc-reviewer/
├── backend/
│   ├── routers/
│   │   ├── documents.py       # Загрузка и структура документа
│   │   ├── instructions.py    # Управление разделами, отметки ложных срабатываний
│   │   ├── evaluation.py      # Оценка через LLM (SSE)
│   │   ├── snapshots.py       # Снимки и сравнение версий
│   │   ├── groups.py          # Продуктовые группы снимков
│   │   └── config_router.py   # Модели, критерии
│   ├── services/
│   │   ├── parser/            # Парсеры PDF, DOCX, MD, TXT
│   │   ├── evaluator.py       # LLM-клиент (OpenAI + Anthropic)
│   │   └── detector.py        # Детектор инструкций (pymorphy3)
│   ├── database.py            # Модели SQLAlchemy, инициализация БД, миграции
│   ├── config.py              # Работа с моделями через БД
│   └── main.py                # FastAPI app, lifespan
├── frontend/
│   └── src/
│       ├── components/        # DocumentTree
│       ├── context/           # DocumentContext (глобальное состояние)
│       ├── pages/             # EvaluationPage, SnapshotsPage, SettingsPage
│       └── styles/            # CSS
├── docs/
│   ├── development/           # Заметки по разработке
│   └── verification/          # Чеклисты проверки этапов
├── criteria.md                # Дефолтные критерии оценки (seed)
├── models.yml                 # Дефолтные LLM-модели (seed)
├── requirements.txt
├── run_dev.py                 # Запуск в режиме разработки
├── run_prod.py                # Точка входа для PyInstaller
├── doc_reviewer.spec          # Конфиг PyInstaller
└── build.bat                  # Скрипт сборки .exe (Windows)
```

---

## Сборка .exe

```bash
build.bat
```

Скрипт собирает фронтенд (`npm run build`), устанавливает зависимости Python и запускает PyInstaller. Результат: `dist\doc-reviewer.exe`.

При запуске `.exe` автоматически стартует бэкенд и открывает браузер. Данные хранятся в папке `data\` рядом с `.exe`.

---

## База данных

SQLite, файл `data/db.sqlite`. Создаётся автоматически при первом запуске.

При изменении схемы БД (добавление таблиц или полей) — удалите `db.sqlite` и перезапустите приложение. Миграции выполняются автоматически при старте.

---

## Известные ограничения

- **Python 3.11** — PyMuPDF несовместим с Python 3.12+
- **Иконки в PDF** — символы из символьных шрифтов (Private Use Area), реализованные как графика, не извлекаются PyMuPDF. В системном промпте LLM явно указано игнорировать такие артефакты при оценке
- **Только локальный запуск** — приложение рассчитано на одного пользователя, многопользовательский режим не поддерживается

---

## Разработка

### Статус этапов

- [x] Этап 1: Скелет приложения
- [x] Этап 2: Парсинг документов
- [x] Этап 3: Идентификация инструкций
- [x] Этап 4: Оценка через LLM + SSE
- [x] Этап 5: Снимки и продуктовые группы
- [x] Этап 6: UX, панель просмотра, XLS-экспорт
- [x] Этап 7: Управление моделями и критериями через UI
- [x] Этап 8: Сборка в .exe

### Запуск тестов

Тесты не реализованы. Проверка выполняется вручную по чеклистам в `docs/verification/`.

### Отладочные скрипты

| Скрипт | Назначение |
|---|---|
| `debug_pua.py` | Поиск PUA-символов в PDF |
| `debug_pua2.py` | Все нестандартные символы в PDF |
| `debug_block.py` | Структура блоков PDF с PUA |
| `debug_content.py` | Контент разделов в БД |
| `debug_db.py` | Проверка наличия меток `[иконка]` в БД |
